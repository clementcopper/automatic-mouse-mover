# Session — 2026-09-03

## Decisions
- Learnings split: stories in LEARNINGS.md, one-liners in `.claude/rules/` scoped by path; CLAUDE.md keeps what it already had, nothing new goes there.
- `fix/crashes-macos-and-arm64` merged into master and deleted (local + origin). Work continues on master.
- `.claude/settings.local.json` and `.vscode/` ignored.

## Open items
- Notarisation (99 €/year, Daniel's call).
- `appInfo/icon.svg` still missing; `make icons` ready.
- 1.6.0 field report: CPU cause unexplained; needs `sample`, spindump, `log show` from a reporter.

## Next concrete steps
- On the M2: `git checkout master && git pull --ff-only && git branch -d fix/crashes-macos-and-arm64 && git fetch -p`.
